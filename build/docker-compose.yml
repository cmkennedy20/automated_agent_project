services:
  github-agent:
    build: 
      context: ..
      dockerfile: build/Dockerfile
    env_file:
      - ../build/.env
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./_work/:/actions-runner/_work/code