services:
  github-agent:
    build: ..
    env_file:
      - ./build/.env
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./_work/:/actions-runner/_work/